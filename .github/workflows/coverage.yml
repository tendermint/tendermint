name: Test Coverage
on:
  pull_request:
  push:
    branches:
      - master
      - release/**

      

jobs: 

  split-test-files: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - name: Create a file with all the pkgs
        run: go list ./... > pkgs.txt && ls && cat ./pkgs.txt
      - name: Split pkgs into 4 files
        run: split -n l/4 --additional-suffix=.txt ./pkgs.txt ${{ github.head_ref }}-
      # cache multiple
      - uses: actions/cache@v1
        with:
          path: ./${{ github.head_ref }}-aa.txt
          key: ${{ runner.os }}-${{ github.head_ref }}-aa
      - uses: actions/cache@v1
        with:
          path: ./${{ github.head_ref }}-ab.txt
          key: ${{ runner.os }}-${{ github.head_ref }}-ab
      - uses: actions/cache@v1
        with:
          path: ./${{ github.head_ref }}-ac.txt
          key: ${{ runner.os }}-${{ github.head_ref }}-ac
      - uses: actions/cache@v1
        with:
          path: ./${{ github.head_ref }}-ad.txt
          key: ${{ runner.os }}-${{ github.head_ref }}-ad

  test-coverage-run-1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          make build
      - uses: actions/cache@v1
        with:
          path: ~/go/bin
          key: ${{ runner.os }}-${{ github.head_ref }}-aa
      - name: test & coverage report creation
        run: |
          cat ${{ github.head_ref }}-aa.txt | xargs go test -mod=readonly -timeout 15m -race -coverprofile=coverage.txt -covermode=atomic
      - uses: codecov/codecov-action@v1
        with:
          file: ./coverage.txt
          fail_ci_if_error: true

  test-coverage-run-2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          make build
      - uses: actions/cache@v1
        with:
          path: ~/go/bin
          key: ${{ runner.os }}-${{ github.head_ref }}-ab
      - name: test & coverage report creation
        run: |
          cat ${{ github.head_ref }}-ab.txt | xargs go test -mod=readonly -timeout 15m -race -coverprofile=coverage.txt -covermode=atomic
      - uses: codecov/codecov-action@v1
        with:
          file: ./coverage.txt
          fail_ci_if_error: true

  test-coverage-run-3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          make build
      - uses: actions/cache@v1
        with:
          path: ~/go/bin
          key: ${{ runner.os }}-${{ github.head_ref }}-ac
      - name: test & coverage report creation
        run: |
          cat ${{ github.head_ref }}-ac.txt | xargs go test -mod=readonly -timeout 15m -race -coverprofile=coverage.txt -covermode=atomic
      - uses: codecov/codecov-action@v1
        with:
          file: ./coverage.txt
          fail_ci_if_error: true

  test-coverage-run-4:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          make build
      - uses: actions/cache@v1
        with:
          path: ~/go/bin
          key: ${{ runner.os }}-${{ github.head_ref }}-ad
      - name: test & coverage report creation
        run: |
          cat ${{ github.head_ref }}-ad.txt | xargs go test -mod=readonly -timeout 15m -race -coverprofile=coverage.txt -covermode=atomic
      - uses: codecov/codecov-action@v1
        with:
          file: ./coverage.txt
          fail_ci_if_error: true
