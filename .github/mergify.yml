queue_rules:
  - name: default
    conditions:
      - base=v0.34.x
      - label=S:automerge

pull_request_rules:
  - name: Automerge to master
    conditions:
      - base=v0.34.x
      - label=S:automerge
    actions:
      queue:
        method: squash
        name: default
        commit_message: title+body
